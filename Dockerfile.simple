FROM node:18-alpine

WORKDIR /app

# Copy all files
COPY . .

# Install all dependencies including dev dependencies
RUN npm install

# Build the server
RUN npm run build

# Set environment
ENV NODE_ENV=production

# Expose port
EXPOSE $PORT

# Start the application
CMD ["npm", "start"]